<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".MainActivity">
    <item
        android:id="@+id/action_sort_by"
        android:title="@string/action_sorty_type"
        app:showAsAction="ifRoom">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/action_sort_by_popularity"
                    android:title="@string/action_sorty_popularity" />
                <item
                    android:id="@+id/action_sort_by_rating"
                    android:title="@string/action_sorty_rating" />
            </group>
        </menu>
    </item>
</menu>
